<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secrets</title>
</head>
<body>
    <h1>Secrets</h1>
    
    <% if (usersWithSecrets && usersWithSecrets.length > 0) { %>
        <ul>
            <% usersWithSecrets.forEach(user => { %>
                <li><%= user.secret %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No secrets available yet.</p>
    <% } %>

    <form action="/submit" method="POST">
        <input type="text" name="secret" placeholder="Share a secret" required>
        <button type="submit">Submit</button>
    </form>
    
    <br><a href="/logout">Logout</a>
</body>
</html>
